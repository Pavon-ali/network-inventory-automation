## 📌 Overview

This project automates the synchronization of network inventory data across multiple VLAN sheets using the Google Sheets API.

The script reads from a centralized Master Equipment Sheet and automatically populates VLAN-specific DHCP sheets based on IP address matching.

This eliminates manual data entry and ensures accurate asset tracking across network segments.
